import { NextPage } from 'next';
import Registered from '../components/registered/Registered';

const Registered_Account: NextPage = () => {
  return <Registered />;
};

export default Registered_Account;
